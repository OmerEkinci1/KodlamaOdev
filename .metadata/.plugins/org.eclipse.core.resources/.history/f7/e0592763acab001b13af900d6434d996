package Core;

public class EntityRepositoryBase<Entity> implements<Entity> {
	
	@Override
    public Entity add(Entity entity) {
        return entity;
    }

    @Override
    public Entity delete(Entity entity) {
        return entity;
    }

    @Override
    public Entity update(Entity entity) {
        return entity;
    }
}
